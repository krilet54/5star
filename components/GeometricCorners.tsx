/**
 * Standardized Geometric Corner Frames for all sections
 * Ensures uniform decorative elements across the site
 */
export default function GeometricCorners() {
  // Decorations are removed site-wide to avoid visual overlap.
  // If you want a single global instance later, reintroduce a lightweight
  // version in the root layout or enable via a prop.
  return null
}
